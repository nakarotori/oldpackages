--- a/Makefile
+++ b/Makefile
@@ -65,6 +65,7 @@ CFLAGS += -Dlinux \
 	 -Wmissing-prototypes \
 	 -DINSTALL_PREFIX="\"$(INSTALL_PREFIX)\"" \
 	 -DCROSS="\"$(CROSS)\"" \
+	 -D_GNU_SOURCE \
 	 $(DEBUG)
 
 ifneq ($(DEF_VMLINUX),)
